# Para desarrollo local (fuera de Docker):
# DATABASE_URL=sqlite:///./requisiciones.db

# Para despliegue Docker (ruta al volumen persistente):
DATABASE_URL=sqlite:////app/data/requisiciones.db

SECRET_KEY=change-me-generate-a-real-key
APP_NAME=Sistema de Requisiciones MVP
